# Contributing

1. Run `uv sync`.
2. Make focused changes that preserve module boundaries.
3. Add tests for behavior and failure modes.
4. Run `make format lint typecheck test security`.
5. Update docs and ADRs when architecture changes.
